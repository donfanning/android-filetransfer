(function() {
    'use strict';

    angular.module('AndroidFiletransfer')
        .controller('settingsController', ['settingsService', SettingsController]);

    function SettingsController(settingsService) {
        // Empty controller right now
    }
})();
